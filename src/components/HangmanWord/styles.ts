import styled from "styled-components";

export const Wrapper = styled.div`
  display: flex;
  gap: 0.5rem;
  justify-content: center;
  align-items: center;
  font-size: 3rem;
  text-transform: uppercase;
  font-family: monospace;
  font-weight: bold;
`;

export const CharBlock = styled.span`
  border-bottom: 3px solid #fff;
`;
